# Variables in this file are overriden by environment variables, if present
parameters:

    environment.defaults:

        DATABASE_URL: mysql://root:@127.0.0.1:3306/pulpy
        INITIALIZATION_MODE: FALSE
